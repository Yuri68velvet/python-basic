#The function simple_interest(p,t,r) is called with the keybord
#arguments the order of arguments doesn't matter in this case


#formule

#Simple Interest=(principal)*(rate)*(#of periods)

def simple_interest(a,b,c):
    return (a*b*c)/100

print(simple_interest(a=10,b=30,c=40))